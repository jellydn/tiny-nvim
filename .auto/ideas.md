# Deferred ideas (nvim 0.13 migration / UX)

- vscode.lua: duplicate `<leader>fw` normal-mode maps (lines 42–48) — clean up
- Ensure oil `init` / keys fire early enough before first BufEnter on a directory
- Consider documenting: reload Cursor/VS Code window after nvim.dir fix so `-` remaps apply
